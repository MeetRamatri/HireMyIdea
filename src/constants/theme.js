export const theme = {
  colors: {
    primary: {
      DEFAULT: '#2D7A4F', // primary green
    },
    accent: {
      DEFAULT: '#A8D96C', // accent lime
    },
    neutral: {
      cream: '#F9F5EE', // neutral cream
    },
    dark: {
      DEFAULT: '#1A1A1A', // dark text
    },
    muted: {
      DEFAULT: '#6B7280', // muted gray
    }
  },
  fontFamily: {
    sans: ['Inter', 'sans-serif'],
  }
};
